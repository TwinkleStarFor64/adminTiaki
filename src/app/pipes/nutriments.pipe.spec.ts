import { NutrimentsPipe } from './nutriments.pipe';

describe('NutrimentsPipe', () => {
  it('create an instance', () => {
    const pipe = new NutrimentsPipe();
    expect(pipe).toBeTruthy();
  });
});
