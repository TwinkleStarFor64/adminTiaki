import { GestionUtilisateursPipe } from './gestion-utilisateurs.pipe';

describe('GestionUtilisateursPipe', () => {
  it('create an instance', () => {
    const pipe = new GestionUtilisateursPipe();
    expect(pipe).toBeTruthy();
  });
});
